import ClearComponent from "./ClearComponent";
import SVGComponent from "./SVGComponent";
import { CloseIcon, PlusIcon } from "./svgIcons";

const icons = {
  compress: SVGComponent,
  delete: ClearComponent,
  close: CloseIcon,
  plus: PlusIcon
};

export default icons;
